// Money helpers: minor-unit arithmetic, formatting, and FX conversion (see spec 9.14).

export function formatMinor(_amountMinor: number, _currencyCode: string, _locale = "en-US"): string {
  return "";
}

export function convert(
  _amountMinor: number,
  _fromCode: string,
  _toCode: string,
  _rate: number,
): number {
  return 0;
}
